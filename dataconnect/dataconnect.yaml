specVersion: "v1"
serviceId: "booking-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "booking-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
